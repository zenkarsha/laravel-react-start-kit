import Layout from "@/components/Layout";

const Home = () => {
    return (
        <Layout>
            <main style={{ padding: 24 }}>
                <h1 style={{ fontSize: 28, marginBottom: 8 }}>
                    Hello — Inertia + React!
                </h1>
            </main>
        </Layout>
    );
};

export default Home;
